#ifndef LONGESTLINE_H
#define LONGESTLINE_H
#include "header/exit_status.h"

int get_next_line(char line[], int maxline);
EXIT_STATUS copy(char from[], char to[]);
EXIT_STATUS longest_line();

#endif
